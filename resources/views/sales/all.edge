@layout('main')
@section('title')
<h4 class="page-title pull-left">Pedidos</h4>
@endsection
@section('area')
<li><a href="/sale">pedidos</a></li>
@endsection
@section('function')
<li><span>todos</span></li>  
@endsection
@section('content')
<h4 class="header-title">Todos os pedidos</h4>
<div class="data-tables">
    <a href="/sale/create" class="btn btn-primary btn-flat btn-lg mt-3">Novo pedido</a>
    <table id="dataTable" class="text-center">
        <thead class="bg-light text-capitalize">
            <tr>
                <th>#</th>
                <th>Status</th>
                <th>Pedido</th>
                <th>Descrição</th>
                <th>Cliente</th>
                <th>Endereço</th>
                <th>Criado por</th>
                <th>Status</th>
            </tr>
        </thead>
        <tbody>
            @each(sale in sales)
                    <tr>
                        <td>#{{sale.sid}}</td>
                        <td>{{sale.ready}}</td>
                        <td>{{sale.amount}} x {{sale.product}}</td>
                        <td>{{sale.description}}</td>
                        <td>{{sale.name}}</td>
                        <td>{{sale.street}}, {{sale.number}}, {{sale.neighborhood}}, {{sale.city}} || {{sale.reference}}</td>
                        <td>{{sale.user}}, <b>em</b> {{sale.created_at}}</td>
                        <td>
                            <a href="/sale/checkout/{{sale.sid}}" class="btn btn-primary mt-4 pr-4 pl-4">Concluir pedido</a>
                        </td>
                    </tr>
            @endeach
        </tbody>
    </table>
</div>
@endsection